>>> 'a b   c'.split(' ')
['a', 'b', '', '', 'c']
正常的切分代码，无法识别连续的空格

>>> re.split(r'\s+', 'a b   c') 
['a', 'b', 'c']
r是不用管转义的那个
\s可以匹配一个空格（也包括Tab等空白符），
   所以\s+表示至少有一个空格，例如匹配' '，' '等

无论多少个空格都可以正常分割。加入,试试：

>>> re.split(r'[\s\,]+', 'a,b, c  d')
['a', 'b', 'c', 'd']

再加入;试试：

>>> re.split(r'[\s\,\;]+', 'a,b;; c  d')
['a', 'b', 'c', 'd']
